# Agent Zero
services:
  - type: web
    runtime: image
    name: agent-zero
    repo: docker://ehlowr0ld/agent-zero-run:multitenancy
    scaling:
      minInstances: 1
      maxInstances: 2
      targetMemoryPercent: 90
      targetCPUPercent: 90
    healthCheckPath: /health
